system {
    host-name {{ inventory_hostname }};
    replace:
    name-server {
        {{ dns1 }};
        {{ dns2 }};
    }
    
    services {
      delete: ftp;
      delete: telnet;
      delete: web-management;
      ssh {
        root-login allow;
      }
      netconf {
        ssh;
      }
    }

}
